# Local development with Docker Compose
version: '3.8'

services:
  tempamailo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tempamailo-local
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
